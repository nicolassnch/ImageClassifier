Library necessaire = sklearn, openCV, glob(lecture de repertoir) , numpy


REPRESENTATION : niveau de gris -> "GRAY"

algo_dico_SVC = algo svc
algo_dico_GausianNb = algo Gausian

train_Data label sous forme de tuples (y_train,X_train)

ou y_train est une liste de labbel et X_train liste de Data sous la representation demandé
Les deux vont en pair

